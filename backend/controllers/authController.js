export function login(req, res, next) {
  // TODO
  res.json({ message: "Login route triggered" });
}

export function signup(req, res, next) {
  // TODO
  res.json({ message: "Signup route triggered" });
}
